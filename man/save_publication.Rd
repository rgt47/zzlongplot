% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publication-export.R
\name{save_publication}
\alias{save_publication}
\title{Save Publication-Ready Plot}
\usage{
save_publication(
  plot,
  filename,
  journal = "nature",
  width_mm = NULL,
  height_mm = NULL,
  dpi = NULL,
  format = NULL,
  column_type = "double",
  panel_label = NULL,
  add_label_to_plot = FALSE,
  ...
)
}
\arguments{
\item{plot}{A ggplot object to be saved.}

\item{filename}{Character string specifying the output filename.
File extension determines format if not specified in format parameter.}

\item{journal}{Character string specifying journal name.
Options: "nature", "science", "nejm", "cell", "fda", "ema".}

\item{width_mm}{Numeric. Plot width in millimeters.
If NULL, uses journal's single column width.}

\item{height_mm}{Numeric. Plot height in millimeters.
If NULL, calculated from plot aspect ratio.}

\item{dpi}{Numeric. Resolution in dots per inch.
If NULL, uses journal's preferred DPI.}

\item{format}{Character string specifying file format.
If NULL, detected from filename extension.}

\item{column_type}{Character string. Either "single" or "double" for
journal column specifications.}

\item{panel_label}{Character string. Panel label for multi-panel figures (e.g., "A", "B").}

\item{add_label_to_plot}{Logical. If TRUE, adds panel label directly to plot.}

\item{...}{Additional arguments passed to ggsave().}
}
\value{
Invisible path to saved file.
}
\description{
Exports a ggplot object in publication-ready format with automatic application
of journal-specific specifications.
}
\examples{
library(ggplot2)

# Create example plot
p <- ggplot(mtcars, aes(wt, mpg, color = factor(cyl))) + 
  geom_point() + 
  theme_nature()

# Save for Nature journal
save_publication(p, "figure1.pdf", journal = "nature")

# Save with panel label for multi-panel figure
save_publication(p, "figure1a.pdf", journal = "nature", 
                 panel_label = "A", column_type = "single")

}
